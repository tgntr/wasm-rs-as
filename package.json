{
  "scripts": {
    "build": "asb assembly/contract.ts",
    "runtime": "cargo run",
    "test": "cd tests && cargo integration-test"
  },
  "devDependencies": {
    "asbuild": "^0.2.0",
    "assemblyscript": "^0.19.23",
    "assemblyscript-json": "^1.1.0",
    "typescript": "^4.6.3"
  }
}

{
  "scripts": {
    "build": "asb assembly/contract.ts && cd wasmer_runtime && cargo build",
    "test": "cd wasmer_runtime && cargo integration-test",
    "start": "npm run build && npm run test"
  },
  "devDependencies": {
    "asbuild": "^0.2.0",
    "assemblyscript": "^0.19.23",
    "assemblyscript-json": "^1.1.0",
    "typescript": "^4.6.3"
  }
}
